/** Optional baked-in client ID for store builds. Leave empty for in-app setup. */
export const DEFAULT_OAUTH_CLIENT_ID = '';

export const SHEETS_SCOPE = 'https://www.googleapis.com/auth/spreadsheets';
